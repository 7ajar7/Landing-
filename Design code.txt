
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>The Trip - موقع خدمي وصفحة تعريفية</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins&display=swap" rel="stylesheet" />
  <style>
    /* Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
    }
    body {
      background-color: #0B1437; /* أزرق داكن */
      color: #E6E1DD; /* أبيض مائل للرمادي */
      line-height: 1.6;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }
    a {
      color: #E6C07A; /* ذهبي */
      text-decoration: none;
      transition: color 0.3s ease;
    }
    a:hover {
      color: #FF6F61; /* برتقالي */
    }

    header {
      background-color: #121B47;
      padding: 20px 30px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 2px 5px rgba(0,0,0,0.5);
      position: sticky;
      top: 0;
      z-index: 100;
    }
    header .logo {
      font-size: 1.8rem;
      font-weight: 700;
      color: #E6C07A;
      letter-spacing: 2px;
      user-select: none;
    }
    nav ul {
      list-style: none;
      display: flex;
      gap: 25px;
    }
    nav ul li {
      font-weight: 500;
    }
    nav ul li:hover {
      color: #FF6F61;
      cursor: pointer;
    }

    main {
      flex: 1;
      padding: 40px 20px;
      max-width: 1100px;
      margin: 0 auto;
      width: 100%;
    }

    section {
      margin-bottom: 60px;
      background-color: #162157; /* أزرق داكن فاتح */
      border-radius: 12px;
      padding: 30px 25px;
      box-shadow: 0 5px 15px rgba(0,0,0,0.3);
    }

    section h2 {
      color: #E6C07A;
      font-size: 2rem;
      margin-bottom: 20px;
      border-bottom: 2px solid #E6C07A;
      padding-bottom: 8px;
      user-select: none;
    }

    /* قسم إغراء العميل */
    #services p {
      font-size: 1.1rem;
      margin-bottom: 18px;
      color: #DDD7C9;
    }
    #services ul {
      list-style-type: disc;
      padding-right: 20px;
      color: #FFF8DC;
      margin-left: 15px;
    }
    #services ul li {
      margin-bottom: 10px;
      font-weight: 600;
    }

    /* قسم متابعة الإجراءات */
    #tracking {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }
    #tracking form {
      background-color: #0B1437;
      padding: 20px;
      border-radius: 10px;
      box-shadow: inset 0 0 10px #FF6F61;
      display: flex;
      gap: 15px;
      flex-wrap: wrap;
      align-items: center;
    }
    #tracking input[type="text"] {
      flex: 1;
      padding: 12px 15px;
      border: none;
      border-radius: 8px;
      font-size: 1rem;
      outline: none;
    }
    #tracking button {
      background-color: #E6C07A;
      border: none;
      padding: 12px 25px;
      font-size: 1rem;
      font-weight: 600;
      border-radius: 8px;
      color: #0B1437;
      cursor: pointer;
      transition: background-color 0.3s ease;
      user-select: none;
    }
    #tracking button:hover {
      background-color: #FF6F61;
      color: #FFF8DC;
    }

    #online-status {
      background-color: #202F6A;
      border-radius: 10px;
      padding: 15px 20px;
      color: #B4D3E8;
      font-weight: 600;
      user-select: none;
    }

    #chat {
      background-color: #0B1437;
      border-radius: 10px;
      padding: 15px 20px;
      max-height: 150px;
      overflow-y: auto;
      color: #DDD7C9;
      font-size: 0.95rem;
      font-family: monospace;
      user-select: text;
      border: 1px solid #E6C07A;
    }

    /* قسم ما بعد البيع */
    #after-sales {
      color: #DDD7C9;
    }
    #after-sales ul {
      list-style-type: none;
      padding: 0;
      display: grid;
      grid-template-columns: repeat(auto-fill,minmax(280px,1fr));
      gap: 20px;
    }
    #after-sales li {
      background-color: #121B47;
      border-radius: 10px;
      padding: 20px;
      box-shadow: 0 3px 10px rgba(0,0,0,0.5);
      user-select: none;
      display: flex;
      flex-direction: column;
      gap: 10px;
    }
    #after-sales li .client-name {
      font-weight: 700;
      font-size: 1.1rem;
      color: #E6C07A;
    }
    #after-sales li .client-feedback {
      font-size: 0.95rem;
      color: #EEE9DA;
      flex-grow: 1;
    }
    #after-sales li .client-service {
      font-weight: 600;
      color: #FF6F61;
      font-style: italic;
    }

    /* Footer */
    footer {
      background-color: #121B47;
      color: #B4D3E8;
      text-align: center;
      padding: 18px 20px;
      font-size: 0.9rem;
      user-select: none;
      box-shadow: 0 -2px 8px rgba(0,0,0,0.4);
    }

    /* Responsive */
    @media(max-width: 700px) {
      #tracking form {
        flex-direction: column;
      }
      nav ul {
        gap: 12px;
      }
    }

  </style>
</head>
<body>

<header>
  <div class="logo">The Trip</div>
  <nav>
    <ul>
      <li><a href="#services">خدماتنا</a></li>
      <li><a href="#tracking">متابعة الإجراءات</a></li>
      <li><a href="#after-sales">ما بعد البيع</a></li>
    </ul>
  </nav>
</header>

<main>

  <section id="services">
    <h2>خدماتنا المميزة</h2>
    <p>نحن في The Trip نقدم لك حلول متكاملة تلبي جميع احتياجات رحلتك بخدمات فريدة واحترافية، من التخطيط إلى المتابعة.</p>
    <ul>
      <li>تخطيط رحلة مخصصة حسب رغباتك</li>
      <li>دعم فني مباشر 24/7</li>
      <li>تسهيلات في الإجراءات والتأشيرات</li>
      <li>متابعة مستمرة لحالة رحلتك</li>
      <li>خدمات ما بعد البيع وضمان رضاك التام</li>
    </ul>
  </section>

  <section id="tracking">
    <h2>متابعة الإجراءات وخدمة العملاء</h2>
    <form id="tracking-form" onsubmit="return false;">
      <input type="text" id="tracking-id" placeholder="أدخل رقم المتابعة" required />
      <button type="submit" onclick="trackOrder()">تابع الآن</button>
    </form>
    <div id="online-status">خدمة العملاء: <span id="status-text">متصل حالياً</span></div>
    <div id="chat">مرحباً! كيف يمكننا مساعدتك اليوم؟</